on: [push]

name: CI

jobs:
  build-zkgroup:
    name: build zkgroup
    strategy:
      matrix:
        target: [x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu, armv7-unknown-linux-gnueabihf]
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Setup rust nightly toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          target: ${{ matrix.target }}
          override: true

      - name: Install the compiler for the aarch64 architecture
        if: ${{ matrix.target == 'aarch64-unknown-linux-gnu' }}
        run: sudo apt install gcc-aarch64-linux-gnu

      - name: Install the compiler for the armhf architecture
        if: ${{ matrix.target == 'armv7-unknown-linux-gnueabihf' }}
        run: sudo apt install gcc-arm-linux-gnueabihf

      - name: Build zkgroup library
        uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --target=${{ matrix.target }} --manifest-path lib/zkgroup/Cargo.toml

      - name: List build output
        run: ls -la lib/zkgroup/target/${{ matrix.target }}/debug

      - name: Upload built artifact
        uses: actions/upload-artifact@v2
        with:
          name: zkgroup-debug-${{ matrix.target }}
          path: lib/zkgroup/target/${{ matrix.target }}/debug/libzkgroup.so
          if-no-files-found: error
