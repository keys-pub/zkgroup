on: [push]

name: CI

jobs:
  build-zkgroup:
    name: build zkgroup
    strategy:
      matrix:
        target: [x86_64-unknown-linux-gnu]
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Setup rust nightly toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          target: ${{ matrix.target }}
          override: true

      - name: Build zkgroup library
        uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --target=${{ matrix.target }} --manifest-path lib/zkgroup/Cargo.toml

      - name: Debug
        run: |
          pwd
          ls -la lib/zkgroup/target/${{ matrix.target }}
          ls -la lib/zkgroup/target/${{ matrix.target }}/debug
          ls -la lib/zkgroup/target/${{ matrix.target }}/debug/build

      - name: Upload built artifact
        uses: actions/upload-artifact@v2
        with:
          name: zkgroup-debug-${{ matrix.target }}
          path: lib/zkgroup/target/${{ matrix.target }}/debug/libzkgroup.so
          if-no-files-found: error
